[core]
	excludesfile = /Users/noviankristianto/.gitignore_global

[difftool "sourcetree"]
	cmd = opendiff \"$LOCAL\" \"$REMOTE\"
	path = 

[user]
	name = novian kristianto
	email = kristianto.novian@gmail.com

[commit]
	template = /Users/noviankristianto/.stCommitMsg

[push]
	default = current

[pull]
	rebase = true

[alias]
	s = status
	b = branch
	a = add
	c = commit
	d = diff
	r = reset
	rb = rebase
	co = checkout
	cp = cherry-pick
	lg = log
	ls = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%C(bold blue)<%an>%Creset' --abbrev-commit
	dt = diff-tree --no-commit-id --name-only -r

[includeIf "gitdir:~/workspace/brank.as/"]
  	path = .gitconfig-work

